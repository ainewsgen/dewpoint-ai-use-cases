services:
  - type: web
    name: dewpoint-ai-use-cases
    env: node
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.10.0
      - key: PORT
        value: 10000
    plan: free
    branch: main
    autoDeploy: true
